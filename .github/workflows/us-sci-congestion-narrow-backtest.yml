name: 🧱 US SCI Narrow Congestion Backtest

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt openpyxl

      - name: Run narrow SCI-style congestion backtest
        run: python us_sci_congestion_narrow_backtest.py

      - name: Upload narrow backtest results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: us-sci-narrow-backtest-${{ github.run_number }}
          path: backtest_us_sci_narrow_results/
          retention-days: 30
